import 'package:flutter/material.dart';

class RabLogo extends StatelessWidget {
  const RabLogo({super.key});
  @override
  Widget build(context) {
    return Image.asset(
      'assets/images/Rent-and-Borrow-logos_black.png',
      width: 350,
      //height: 200,
    );
  }
}
